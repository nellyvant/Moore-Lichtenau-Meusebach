mtry = 3,
importance = TRUE
)
# Modellübersicht -> man kann ableiten, wie wichtig die einzelnen Features waren (also: MTN, MTS, Hour_sin,Weekday, DayOfYear ...)
mod_rf
importance(mod_rf)
# %IncMSE zeigt an, wie stark der Fehler steigt, wenn ein Feature zufällig permutiert wird -> Je höher der Wert, desto wichtiger ist das Feature für die Gesamtvorhersage
# NodePurity sagt strukturbezogen, wie wichtig ein Feature für die Baumaufteilung ist
pred_rf <- predict(mod_rf, newdata = wide)
wide$MTW_filled_rf <- ifelse(
is.na(wide$MTW),
pred_rf,
wide$MTW
)
# RMSE validieren (wie vorher)
set.seed(42)
n_reps <- 5 # Anzahl der Wiederholungen
rmse_vals <- numeric(n_reps) # speichert die werte der 5 Wiederholungen
for(i in 1:n_reps){
valid_idx <- which(!is.na(wide$MTW)) # nur echte Messwerte auswählen
idx <- sample(
valid_idx,
size = floor(0.2 * length(valid_idx)) # zufällig 20% der Werte auswählen
)
wide_test <- wide # Kopie erzeugen
wide_test$MTW[idx] <- NA # künstlich 20% auf NA setzen
pred_test_rf <- predict(mod_rf, newdata = wide_test) # Vorhersage mit Modell
rmse_vals[i] <- sqrt(mean(
(pred_test_rf[idx] - wide$MTW[idx])^2
)) # RMSE berechnen
cat("Wiederholung", i, "fertig\n") # Fortschrittsanzeige unten in der Console
}
mean_rmse <- mean(rmse_vals) # Mittelwert
sd_rmse   <- sd(rmse_vals) # Standardabweichung
nrmse     <- mean_rmse / sd(wide$MTW, na.rm = TRUE) # Fehler relativ zur natürlichen Streuung
mean_rmse # Relative Luftfeuchte: weicht ca. 1,503 % von den echten Werten ab
sd_rmse # SD: 0,027 -> bei 5 Wiederholungen sehr ähnliche identische Ergebnisse = stabiles Modell, keine Zufallseffekte
nrmse # NRMSE: 0,093 -> Fehler ist relativ klein im Verhältnis zur natürlichen Variabilität
# erzeugte Daten wieder in die Originaltabelle einfügen
mtw_filled_long_RL <- wide %>%
select(DateTime, MTW_filled_rf) %>%
rename(relLF_percent = MTW_filled_rf) %>%
mutate(Logger_ID = "MTW")
mtw_filled_long_renamed <- mtw_filled_long_RL %>%
rename(relLF_percent_filled = relLF_percent)
all_loggers_filled <- all_loggers_filled %>%
left_join(
mtw_filled_long_renamed %>% select(DateTime, Logger_ID, relLF_percent_filled),
by = c("DateTime", "Logger_ID")
) %>%
mutate(
relLF_percent = case_when(
Logger_ID == "MTW" & is.na(relLF_percent) ~ round(relLF_percent_filled, 2),
TRUE ~ round(relLF_percent, 2)  # auch die übrigen Werte runden
)
) %>%
select(-relLF_percent_filled)
# Variable definieren
var <- "Taupunkt_C"
# Datensatz vorbereiten wie vorher
wide <- all_loggers %>%
select(DateTime, Standort, Logger_ID, all_of(var)) %>%
filter(Standort == "MT") %>%
pivot_wider(names_from = Logger_ID,
values_from = all_of(var)) %>%
arrange(DateTime)
# Zeitliche Features hinzufügen
wide$Hour <- as.numeric(format(wide$DateTime, "%H"))
wide$Weekday <- as.factor(weekdays(wide$DateTime))
wide$DayOfYear <- as.numeric(format(wide$DateTime, "%j"))
wide$Hour_sin <- sin(2 * pi * wide$Hour / 24)
wide$Hour_cos <- cos(2 * pi * wide$Hour / 24)
train <- wide %>%
filter(!is.na(MTW))
# Random Forest Modell erstellen
mod_rf <- randomForest(
MTW ~ MTN + MTS + MTO + Hour_sin + Hour_cos + Weekday + DayOfYear,
data = train,
ntree = 500,        # je mehr, desto stabiler ist das Model
mtry = 3,
importance = TRUE
)
# Modellübersicht -> man kann ableiten, wie wichtig die einzelnen Features waren (also: MTN, MTS, Hour_sin,Weekday, DayOfYear ...)
mod_rf
importance(mod_rf)
# %IncMSE zeigt an, wie stark der Fehler steigt, wenn ein Feature zufällig permutiert wird -> Je höher der Wert, desto wichtiger ist das Feature für die Gesamtvorhersage
# NodePurity sagt strukturbezogen, wie wichtig ein Feature für die Baumaufteilung ist
pred_rf <- predict(mod_rf, newdata = wide)
wide$MTW_filled_rf <- ifelse(
is.na(wide$MTW),
pred_rf,
wide$MTW
)
# RMSE validieren (wie vorher)
set.seed(42)
n_reps <- 5 # Anzahl der Wiederholungen
rmse_vals <- numeric(n_reps) # speichert die werte der 5 Wiederholungen
for(i in 1:n_reps){
valid_idx <- which(!is.na(wide$MTW)) # nur echte Messwerte auswählen
idx <- sample(
valid_idx,
size = floor(0.2 * length(valid_idx)) # zufällig 20% der Werte auswählen
)
wide_test <- wide # Kopie erzeugen
wide_test$MTW[idx] <- NA # künstlich 20% auf NA setzen
pred_test_rf <- predict(mod_rf, newdata = wide_test) # Vorhersage mit Modell
rmse_vals[i] <- sqrt(mean(
(pred_test_rf[idx] - wide$MTW[idx])^2
)) # RMSE berechnen
cat("Wiederholung", i, "fertig\n") # Fortschrittsanzeige unten in der Console
}
mean_rmse <- mean(rmse_vals) # Mittelwert
sd_rmse   <- sd(rmse_vals) # Standardabweichung
nrmse     <- mean_rmse / sd(wide$MTW, na.rm = TRUE) # Fehler relativ zur natürlichen Streuung
mean_rmse # Relative Luftfeuchte: weicht ca. 1,503 % von den echten Werten ab
sd_rmse # SD: 0,027 -> bei 5 Wiederholungen sehr ähnliche identische Ergebnisse = stabiles Modell, keine Zufallseffekte
nrmse # NRMSE: 0,093 -> Fehler ist relativ klein im Verhältnis zur natürlichen Variabilität
# erzeugte Daten wieder in die Originaltabelle einfügen
mtw_filled_long_Tau <- wide %>%
select(DateTime, MTW_filled_rf) %>%
rename(Taupunkt_C = MTW_filled_rf) %>%
mutate(Logger_ID = "MTW")
mtw_filled_long_renamed <- mtw_filled_long_Tau %>%
rename(Taupunkt_C_filled = Taupunkt_C)
all_loggers_filled <- all_loggers_filled %>%
left_join(
mtw_filled_long_renamed %>% select(DateTime, Logger_ID, Taupunkt_C_filled),
by = c("DateTime", "Logger_ID")
) %>%
mutate(
Taupunkt_C = case_when(
Logger_ID == "MTW" & is.na(Taupunkt_C) ~ round(Taupunkt_C_filled, 2),
TRUE ~ round(Taupunkt_C, 2)  # auch die übrigen Werte runden
)
) %>%
select(-Taupunkt_C_filled)
View(mtw_filled_long_RL)
View(mtw_filled_long_renamed)
# Variable definieren
var <- "relLF_percent"
# Datensatz vorbereiten wie vorher
wide <- all_loggers %>%
select(DateTime, Standort, Logger_ID, all_of(var)) %>%
filter(Standort == "MN") %>%
pivot_wider(names_from = Logger_ID,
values_from = all_of(var)) %>%
arrange(DateTime)
# Zeitliche Features hinzufügen
wide$Hour <- as.numeric(format(wide$DateTime, "%H"))
wide$Weekday <- as.factor(weekdays(wide$DateTime))
wide$DayOfYear <- as.numeric(format(wide$DateTime, "%j"))
wide$Hour_sin <- sin(2 * pi * wide$Hour / 24)
wide$Hour_cos <- cos(2 * pi * wide$Hour / 24)
train <- wide %>%
filter(!is.na(MNW))
# Random Forest Modell erstellen
mod_rf <- randomForest(
MNW ~ MNN + MNS + MNM + Hour_sin + Hour_cos + Weekday + DayOfYear,
data = train,
ntree = 500,        # je mehr, desto stabiler ist das Model
mtry = 3,
importance = TRUE
)
mod_rf
importance(mod_rf)
# %IncMSE zeigt an, wie stark der Fehler steigt, wenn ein Feature zufällig
pred_rf <- predict(mod_rf, newdata = wide)
pred_rf <- round(pred_rf, 2)
wide$MNW_filled_rf <- ifelse(
is.na(wide$MNW),
pred_rf,
wide$MNW
)
# RMSE validieren (wie vorher)
set.seed(42)
n_reps <- 5
rmse_vals <- numeric(n_reps)
for(i in 1:n_reps){
valid_idx <- which(!is.na(wide$MNW))
idx <- sample(
valid_idx,
size = floor(0.2 * length(valid_idx))
)
wide_test <- wide
wide_test$MNW[idx] <- NA
pred_test_rf <- predict(mod_rf, newdata = wide_test)
rmse_vals[i] <- sqrt(mean(
(pred_test_rf[idx] - wide$MNW[idx])^2
))
cat("Wiederholung", i, "fertig\n")
}
mean_rmse <- mean(rmse_vals)
sd_rmse   <- sd(rmse_vals)
nrmse     <- mean_rmse / sd(wide$MNW, na.rm = TRUE)
# -> ändert sich bei wiederholten Durchlauf
mean_rmse # Luftfeuchtigkeit: weicht ca. 0,72 % von den echten Werten ab
sd_rmse # SD: 0,014 -> bei 5 Wiederholungen fast identische Ergebnisse = stabiles Modell, keine Zufallseffekte
nrmse # Fehler relativ zur natürlichen Streuung: 0,047 -> Fehler ist relativ klein im Verhältnis zur natürlichen Variabilität
mnw_filled_long_LF <- wide %>%
select(DateTime, MNW_filled_rf) %>%
rename(relLF_percent = MNW_filled_rf) %>%
mutate(Logger_ID = "MNW")
mnw_filled_long_renamed <- mnw_filled_long_LF %>%
rename(relLF_percent_filled = relLF_percent)
all_loggers_filled <- all_loggers_filled %>%
left_join(
mnw_filled_long_renamed %>% select(DateTime, Logger_ID, relLF_percent_filled),
by = c("DateTime", "Logger_ID")
) %>%
mutate(
relLF_percent = case_when(
Logger_ID == "MNW" & is.na(relLF_percent) ~ round(relLF_percent_filled, 2),
TRUE ~ round(relLF_percent, 2)  # auch die übrigen Werte runden
)
) %>%
select(-relLF_percent_filled)
# Variable definieren
var <- "Taupunkt_C"
# Datensatz vorbereiten wie vorher
wide <- all_loggers %>%
select(DateTime, Standort, Logger_ID, all_of(var)) %>%
filter(Standort == "MN") %>%
pivot_wider(names_from = Logger_ID,
values_from = all_of(var)) %>%
arrange(DateTime)
# Zeitliche Features hinzufügen
wide$Hour <- as.numeric(format(wide$DateTime, "%H"))
wide$Weekday <- as.factor(weekdays(wide$DateTime))
wide$DayOfYear <- as.numeric(format(wide$DateTime, "%j"))
wide$Hour_sin <- sin(2 * pi * wide$Hour / 24)
wide$Hour_cos <- cos(2 * pi * wide$Hour / 24)
train <- wide %>%
filter(!is.na(MNW))
# Random Forest Modell erstellen
mod_rf <- randomForest(
MNW ~ MNN + MNS + MNM + MNO + Hour_sin + Hour_cos + Weekday + DayOfYear,
data = train,
ntree = 500,        # je mehr, desto stabiler ist das Model
mtry = 3,
importance = TRUE
)
mod_rf
importance(mod_rf)
# %IncMSE zeigt an, wie stark der Fehler steigt, wenn ein Feature zufällig
pred_rf <- predict(mod_rf, newdata = wide)
pred_rf <- round(pred_rf, 2)
wide$MNW_filled_rf <- ifelse(
is.na(wide$MNW),
pred_rf,
wide$MNW
)
# RMSE validieren (wie vorher)
set.seed(42)
n_reps <- 5
rmse_vals <- numeric(n_reps)
for(i in 1:n_reps){
valid_idx <- which(!is.na(wide$MNW))
idx <- sample(
valid_idx,
size = floor(0.2 * length(valid_idx))
)
wide_test <- wide
wide_test$MNW[idx] <- NA
pred_test_rf <- predict(mod_rf, newdata = wide_test)
rmse_vals[i] <- sqrt(mean(
(pred_test_rf[idx] - wide$MNW[idx])^2
))
cat("Wiederholung", i, "fertig\n")
}
mean_rmse <- mean(rmse_vals)
sd_rmse   <- sd(rmse_vals)
nrmse     <- mean_rmse / sd(wide$MNW, na.rm = TRUE)
mean_rmse # Luftfeuchtigkeit: weicht ca. 0,72 % von den echten Werten ab
sd_rmse # SD: 0,014 -> bei 5 Wiederholungen fast identische Ergebnisse = stabiles Modell, keine Zufallseffekte
nrmse # Fehler relativ zur natürlichen Streuung: 0,047 -> Fehler ist relativ klein im Verhältnis zur natürlichen Variabilität
# erzeugte Daten wieder in die Originaltabelle einfügen
mnw_filled_long_Tau <- wide %>%
select(DateTime, MNW_filled_rf) %>%
rename(Taupunkt_C = MNW_filled_rf) %>%
mutate(Logger_ID = "MNW")
mnw_filled_long_renamed <- mnw_filled_long_Tau %>%
rename(Taupunkt_C_filled = Taupunkt_C)
all_loggers_filled <- all_loggers_filled %>%
left_join(
mnw_filled_long_renamed %>% select(DateTime, Logger_ID, Taupunkt_C_filled),
by = c("DateTime", "Logger_ID")
) %>%
mutate(
Taupunkt_C = case_when(
Logger_ID == "MNW" & is.na(Taupunkt_C) ~ round(Taupunkt_C_filled, 2),
TRUE ~ round(Taupunkt_C, 2)  # auch die übrigen Werte runden
)
) %>%
select(-Taupunkt_C_filled)
# Variable definieren
var <- "Temp_schatten_C"
# Datensatz vorbereiten wie vorher
wide <- all_loggers %>%
select(DateTime, Standort, Logger_ID, all_of(var)) %>%
filter(Standort == "MN") %>%
pivot_wider(names_from = Logger_ID,
values_from = all_of(var)) %>%
arrange(DateTime)
# Zeitliche Features hinzufügen
wide$Hour <- as.numeric(format(wide$DateTime, "%H"))
wide$Weekday <- as.factor(weekdays(wide$DateTime))
wide$DayOfYear <- as.numeric(format(wide$DateTime, "%j"))
wide$Hour_sin <- sin(2 * pi * wide$Hour / 24)
wide$Hour_cos <- cos(2 * pi * wide$Hour / 24)
train <- wide %>%
filter(!is.na(MNW))
# Random Forest Modell erstellen
mod_rf <- randomForest(
MNW ~ MNN + MNS + MNM + MNO + Hour_sin + Hour_cos + Weekday + DayOfYear,
data = train,
ntree = 500,        # je mehr, desto stabiler ist das Model
mtry = 3,
importance = TRUE
)
mod_rf
importance(mod_rf)
# %IncMSE zeigt an, wie stark der Fehler steigt, wenn ein Feature zufällig
pred_rf <- predict(mod_rf, newdata = wide)
pred_rf <- round(pred_rf, 2)
wide$MNW_filled_rf <- ifelse(
is.na(wide$MNW),
pred_rf,
wide$MNW
)
# RMSE validieren (wie vorher)
set.seed(42)
n_reps <- 5
rmse_vals <- numeric(n_reps)
for(i in 1:n_reps){
valid_idx <- which(!is.na(wide$MNW))
idx <- sample(
valid_idx,
size = floor(0.2 * length(valid_idx))
)
wide_test <- wide
wide_test$MNW[idx] <- NA
pred_test_rf <- predict(mod_rf, newdata = wide_test)
rmse_vals[i] <- sqrt(mean(
(pred_test_rf[idx] - wide$MNW[idx])^2
))
cat("Wiederholung", i, "fertig\n")
}
mean_rmse <- mean(rmse_vals)
sd_rmse   <- sd(rmse_vals)
nrmse     <- mean_rmse / sd(wide$MNW, na.rm = TRUE)
# -> ändert sich bei wiederholten Durchlauf
mean_rmse # Temperatur (schatten): weicht ca. 0,15 °C von echten Werten ab
sd_rmse # SD: 0,005 -> bei 5 Wiederholungen fast identische Ergebnisse = stabiles Modell, keine Zufallseffekte
nrmse # Fehler relativ zur natürlichen Streuung: 0,037 -> Fehler ist relativ klein im Verhältnis zur natürlichen Variabilität
mnw_filled_long_Tau <- wide %>%
select(DateTime, MNW_filled_rf) %>%
rename(Taupunkt_C = MNW_filled_rf) %>%
mutate(Logger_ID = "MNW")
mnw_filled_long_renamed <- mnw_filled_long_Tau %>%
rename(Taupunkt_C_filled = Taupunkt_C)
all_loggers_filled <- all_loggers_filled %>%
left_join(
mnw_filled_long_renamed %>% select(DateTime, Logger_ID, Taupunkt_C_filled),
by = c("DateTime", "Logger_ID")
) %>%
mutate(
Taupunkt_C = case_when(
Logger_ID == "MNW" & is.na(Taupunkt_C) ~ round(Taupunkt_C_filled, 2),
TRUE ~ round(Taupunkt_C, 2)  # auch die übrigen Werte runden
)
) %>%
select(-Taupunkt_C_filled)
# Variable definieren
var <- "Temp_schatten_C"
# Datensatz vorbereiten wie vorher
wide <- all_loggers %>%
select(DateTime, Standort, Logger_ID, all_of(var)) %>%
filter(Standort == "MN") %>%
pivot_wider(names_from = Logger_ID,
values_from = all_of(var)) %>%
arrange(DateTime)
# Zeitliche Features hinzufügen
wide$Hour <- as.numeric(format(wide$DateTime, "%H"))
wide$Weekday <- as.factor(weekdays(wide$DateTime))
wide$DayOfYear <- as.numeric(format(wide$DateTime, "%j"))
wide$Hour_sin <- sin(2 * pi * wide$Hour / 24)
wide$Hour_cos <- cos(2 * pi * wide$Hour / 24)
train <- wide %>%
filter(!is.na(MNW))
# Random Forest Modell erstellen
mod_rf <- randomForest(
MNW ~ MNN + MNS + MNM + MNO + Hour_sin + Hour_cos + Weekday + DayOfYear,
data = train,
ntree = 500,        # je mehr, desto stabiler ist das Model
mtry = 3,
importance = TRUE
)
# Modellübersicht -> man kann ableiten, wie wichtig die einzelnen Features waren (also: MTN, MTS, Hour_sin,Weekday, DayOfYear ...)
mod_rf
importance(mod_rf)
# %IncMSE zeigt an, wie stark der Fehler steigt, wenn ein Feature zufällig
pred_rf <- predict(mod_rf, newdata = wide)
pred_rf <- round(pred_rf, 2)
wide$MNW_filled_rf <- ifelse(
is.na(wide$MNW),
pred_rf,
wide$MNW
)
# RMSE validieren (wie vorher)
set.seed(42)
n_reps <- 5
rmse_vals <- numeric(n_reps)
for(i in 1:n_reps){
valid_idx <- which(!is.na(wide$MNW))
idx <- sample(
valid_idx,
size = floor(0.2 * length(valid_idx))
)
wide_test <- wide
wide_test$MNW[idx] <- NA
pred_test_rf <- predict(mod_rf, newdata = wide_test)
rmse_vals[i] <- sqrt(mean(
(pred_test_rf[idx] - wide$MNW[idx])^2
))
cat("Wiederholung", i, "fertig\n")
}
mean_rmse <- mean(rmse_vals)
sd_rmse   <- sd(rmse_vals)
nrmse     <- mean_rmse / sd(wide$MNW, na.rm = TRUE)
# -> ändert sich bei wiederholten Durchlauf
mean_rmse # Temperatur (schatten): weicht ca. 0,138 °C von echten Werten ab
sd_rmse # SD: 0,008 -> bei 5 Wiederholungen fast identische Ergebnisse = stabiles Modell, keine Zufallseffekte
nrmse # Fehler relativ zur natürlichen Streuung: 0,027 -> Fehler ist relativ klein im Verhältnis zur natürlichen Variabilität
# erzeugte Daten wieder in die Originaltabelle einfügen
mnw_filled_long_Temp_schatten <- wide %>%
select(DateTime, MNW_filled_rf) %>%
rename(Temp_schatten_C = MNW_filled_rf) %>%
mutate(Logger_ID = "MNW")
mnw_filled_long_renamed <- mnw_filled_long_Temp_schatten %>%
rename(Temp_schatten_C_filled = Temp_schatten_C)
all_loggers_filled <- all_loggers_filled %>%
left_join(
mnw_filled_long_renamed %>% select(DateTime, Logger_ID, Temp_schatten_C_filled),
by = c("DateTime", "Logger_ID")
) %>%
mutate(
Temp_schatten_C = case_when(
Logger_ID == "MNW" & is.na(Temp_schatten_C) ~ round(Temp_schatten_C_filled, 2),
TRUE ~ round(Temp_schatten_C, 2)  # auch die übrigen Werte runden
)
) %>%
select(-Temp_schatten_C_filled)
# Variable definieren
var <- "Temp_sonne_C"
# Datensatz vorbereiten wie vorher
wide <- all_loggers %>%
select(DateTime, Standort, Logger_ID, all_of(var)) %>%
filter(Standort == "MN") %>%
pivot_wider(names_from = Logger_ID,
values_from = all_of(var)) %>%
arrange(DateTime)
# Zeitliche Features hinzufügen
wide$Hour <- as.numeric(format(wide$DateTime, "%H"))
wide$Weekday <- as.factor(weekdays(wide$DateTime))
wide$DayOfYear <- as.numeric(format(wide$DateTime, "%j"))
wide$Hour_sin <- sin(2 * pi * wide$Hour / 24)
wide$Hour_cos <- cos(2 * pi * wide$Hour / 24)
train <- wide %>%
filter(!is.na(MNW))
# Random Forest Modell erstellen
mod_rf <- randomForest(
MNW ~ MNN + MNS + Hour_sin + Hour_cos + Weekday + DayOfYear,
data = train,
ntree = 500,        # je mehr, desto stabiler ist das Model
mtry = 3,
importance = TRUE
)
mod_rf
importance(mod_rf)
# %IncMSE zeigt an, wie stark der Fehler steigt, wenn ein Feature zufällig
pred_rf <- predict(mod_rf, newdata = wide)
pred_rf <- round(pred_rf, 2)
wide$MNW_filled_rf <- ifelse(
is.na(wide$MNW),
pred_rf,
wide$MNW
)
# RMSE validieren (wie vorher)
set.seed(42)
n_reps <- 5
rmse_vals <- numeric(n_reps)
for(i in 1:n_reps){
valid_idx <- which(!is.na(wide$MNW))
idx <- sample(
valid_idx,
size = floor(0.2 * length(valid_idx))
)
wide_test <- wide
wide_test$MNW[idx] <- NA
pred_test_rf <- predict(mod_rf, newdata = wide_test)
rmse_vals[i] <- sqrt(mean(
(pred_test_rf[idx] - wide$MNW[idx])^2
))
cat("Wiederholung", i, "fertig\n")
}
mean_rmse <- mean(rmse_vals)
sd_rmse   <- sd(rmse_vals)
nrmse     <- mean_rmse / sd(wide$MNW, na.rm = TRUE)
# -> ändert sich bei wiederholten Durchlauf
mean_rmse # Temperatur (schatten): weicht ca. 0,138 °C von echten Werten ab
sd_rmse # SD: 0,008 -> bei 5 Wiederholungen fast identische Ergebnisse = stabiles Modell, keine Zufallseffekte
nrmse # Fehler relativ zur natürlichen Streuung: 0,027 -> Fehler ist relativ klein im Verhältnis zur natürlichen Variabilität
